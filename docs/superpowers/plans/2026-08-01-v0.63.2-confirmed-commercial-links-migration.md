# Metabase v0.63.2 Confirmed Commercial Links Migration Implementation Plan

> **2026-08-03 最终覆盖：** “保留 white-label、remote-sync、cloud 商业卡片，仅移除次要链接”的旧规则已废弃。商业推广卡片的标题、说明、按钮、钻石和外链全部不渲染；保留底层功能实现。完整范围见 `2026-08-03-v0.63.2-final-ui-branding-cleanup.md`。

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reapply the remaining commercial-link customizations confirmed in the previous source tree without removing operational, security, legal, or support links.

**Architecture:** Historical implementation record only. The final presentation rule is to render confirmed commercial cards and links as empty while preserving component interfaces and underlying feature implementations.

**Tech Stack:** React, TypeScript, Jest, Testing Library, Bun.

## Global Constraints

- Work on `customization/v0.63.2-migration` based on official `v0.63.2`.
- Limit implementation to differences confirmed by `/Users/chengang/Documents/metabase/metabase-master`.
- Preserve Slack API links, migration destination links, public item links, embedding usage conditions, security documentation, and fault-support email links.
- Do not preserve the commercial primary actions or copy of white-label, remote-sync, cloud, license, trial, upgrade, or plan cards.
- Do not remove SDK quick-start, SDK documentation, or JWT/SSO implementation links for users who already have the SDK feature.
- Do not change feature detection, token checks, tracking, migration state, or settings mutations.

---

### Task 1: Embedded SDK upgrade presentation

**Files:**

- Modify: `frontend/src/metabase/admin/upsells/UpsellSdkLink.tsx`
- Modify: `frontend/src/metabase/admin/settings/components/EmbeddingSettings/EmbeddingSdkSettings/EmbeddingSdkSettings.tsx`
- Modify: `frontend/src/metabase/admin/settings/components/EmbeddingSettings/EmbeddingSettings/tests/enterprise.unit.spec.tsx`

**Interfaces:**

- Consumes: `UpsellSdkLink()` and the existing SDK feature flags.
- Produces: the stable `UpsellSdkLink` export returning `null`, and no API-key information banner when the SDK feature is unavailable.

- [x] **Step 1: Change the enterprise embedding test to reject the unavailable-feature upsell banner**

```tsx
expect(screen.queryByTestId("sdk-settings-alert-info")).not.toBeInTheDocument();
expect(screen.queryByText("upgrade to Metabase Pro")).not.toBeInTheDocument();
```

Keep the premium test that verifies the JWT/SSO implementation link for an enabled SDK feature.

- [x] **Step 2: Run the enterprise test and verify failure**

Run: `bunx jest --runInBand --runTestsByPath frontend/src/metabase/admin/settings/components/EmbeddingSettings/EmbeddingSettings/tests/enterprise.unit.spec.tsx`

Expected: FAIL while the upgrade banner and link still render.

- [x] **Step 3: Restore the confirmed null behavior**

```tsx
export function UpsellSdkLink() {
  return null;
}
```

In `EmbeddingSdkSettings`, remove the `UpsellSdkLink` import and change the `needsToUpgrade: true` match branch to `() => null`. Retain the `needsToImplementJwt` branch and all card links.

- [x] **Step 4: Run enterprise and premium embedding tests**

Run: `bunx jest --runInBand --runTestsByPath frontend/src/metabase/admin/settings/components/EmbeddingSettings/EmbeddingSettings/tests/enterprise.unit.spec.tsx frontend/src/metabase/admin/settings/components/EmbeddingSettings/EmbeddingSettings/tests/premium.unit.spec.tsx`

Expected: PASS.

### Task 2: Commercial cards fully hidden (final override)

**Files:**

- Modify: `frontend/src/metabase/admin/upsells/UpsellWhitelabel.tsx`
- Modify: `frontend/src/metabase/admin/upsells/UpsellRemoteSync.tsx`
- Modify: `frontend/src/metabase/admin/upsells/UpsellCloud.tsx`
- Modify: `frontend/src/metabase/admin/settings/tests/oss.unit.spec.tsx`
- Modify: `frontend/src/metabase/admin/settings/components/SettingsPages/RemoteSyncSettingsPage.unit.spec.tsx`
- Modify: `frontend/src/metabase/admin/settings/components/SettingsPages/CloudSettingsPage.unit.spec.tsx`

**Interfaces:**

- Consumes: existing exported card component names and props.
- Produces: stable exports returning `null`; no title, description, diamond, primary button or secondary link is mounted.

- [x] **Step 1: Add negative assertions for complete card removal**

```tsx
expect(
  screen.queryByText("Make Metabase look like you"),
).not.toBeInTheDocument();
expect(
  screen.queryByRole("link", { name: "Learn more" }),
).not.toBeInTheDocument();

expect(
  screen.queryByText(/Manage your Metabase content in Git/i),
).not.toBeInTheDocument();
expect(
  screen.queryByRole("link", { name: "Learn more" }),
).not.toBeInTheDocument();

expect(screen.queryByText("Migrate to Metabase Cloud")).not.toBeInTheDocument();
expect(
  screen.queryByRole("link", { name: "Learn more" }),
).not.toBeInTheDocument();
```

- [x] **Step 2: Run the three tests against the final behavior**

Run: `bunx jest --runInBand --runTestsByPath frontend/src/metabase/admin/settings/tests/oss.unit.spec.tsx frontend/src/metabase/admin/settings/components/SettingsPages/RemoteSyncSettingsPage.unit.spec.tsx frontend/src/metabase/admin/settings/components/SettingsPages/CloudSettingsPage.unit.spec.tsx`

Expected: PASS when the complete commercial cards are absent.

- [x] **Step 3: Hide the complete commercial presentation**

Use the shared hidden admin upsell exports or null-rendering presentation components. Preserve underlying feature code and caller-compatible exports, but do not mount `UpsellBigCard`, tracking, buttons or links.

- [x] **Step 4: Run the three tests**

Run: `bunx jest --runInBand --runTestsByPath frontend/src/metabase/admin/settings/tests/oss.unit.spec.tsx frontend/src/metabase/admin/settings/components/SettingsPages/RemoteSyncSettingsPage.unit.spec.tsx frontend/src/metabase/admin/settings/components/SettingsPages/CloudSettingsPage.unit.spec.tsx`

Expected: PASS.

### Task 3: Batch verification and audit boundary

**Files:**

- Modify: `docs/superpowers/plans/2026-08-01-v0.63.2-confirmed-commercial-links-migration.md`

**Interfaces:**

- Consumes: Tasks 1-2 and the source-wide admin-link audit.
- Produces: a verified sixth migration batch plus a documented boundary for links intentionally retained.

- [x] **Step 1: Format and run all focused tests**

Run: `bunx oxfmt --write frontend/src/metabase/admin/upsells/UpsellSdkLink.tsx frontend/src/metabase/admin/upsells/UpsellWhitelabel.tsx frontend/src/metabase/admin/upsells/UpsellRemoteSync.tsx frontend/src/metabase/admin/upsells/UpsellCloud.tsx frontend/src/metabase/admin/settings/components/EmbeddingSettings/EmbeddingSdkSettings/EmbeddingSdkSettings.tsx frontend/src/metabase/admin/settings/components/EmbeddingSettings/EmbeddingSettings/tests/enterprise.unit.spec.tsx frontend/src/metabase/admin/settings/tests/oss.unit.spec.tsx frontend/src/metabase/admin/settings/components/SettingsPages/RemoteSyncSettingsPage.unit.spec.tsx frontend/src/metabase/admin/settings/components/SettingsPages/CloudSettingsPage.unit.spec.tsx`

Run: `bunx jest --runInBand --runTestsByPath frontend/src/metabase/admin/settings/components/EmbeddingSettings/EmbeddingSettings/tests/enterprise.unit.spec.tsx frontend/src/metabase/admin/settings/components/EmbeddingSettings/EmbeddingSettings/tests/premium.unit.spec.tsx frontend/src/metabase/admin/settings/tests/oss.unit.spec.tsx frontend/src/metabase/admin/settings/components/SettingsPages/RemoteSyncSettingsPage.unit.spec.tsx frontend/src/metabase/admin/settings/components/SettingsPages/CloudSettingsPage.unit.spec.tsx`

Expected: all focused suites pass.

- [x] **Step 2: Run lint and type checking**

Run: `bunx eslint --max-warnings 0 frontend/src/metabase/admin/upsells/UpsellSdkLink.tsx frontend/src/metabase/admin/upsells/UpsellWhitelabel.tsx frontend/src/metabase/admin/upsells/UpsellRemoteSync.tsx frontend/src/metabase/admin/upsells/UpsellCloud.tsx frontend/src/metabase/admin/settings/components/EmbeddingSettings/EmbeddingSdkSettings/EmbeddingSdkSettings.tsx frontend/src/metabase/admin/settings/components/EmbeddingSettings/EmbeddingSettings/tests/enterprise.unit.spec.tsx frontend/src/metabase/admin/settings/tests/oss.unit.spec.tsx frontend/src/metabase/admin/settings/components/SettingsPages/RemoteSyncSettingsPage.unit.spec.tsx frontend/src/metabase/admin/settings/components/SettingsPages/CloudSettingsPage.unit.spec.tsx`

Run: `bun run type-check-pure`

Expected: both commands exit successfully.

- [x] **Step 3: Scan migrated source and inspect the diff**

Run: `rg -n 'upgrade to Metabase Pro|Learn more|ExternalLink|getDocsUrl|useUpsellLink' frontend/src/metabase/admin/upsells/UpsellSdkLink.tsx frontend/src/metabase/admin/upsells/UpsellWhitelabel.tsx frontend/src/metabase/admin/upsells/UpsellRemoteSync.tsx frontend/src/metabase/admin/upsells/UpsellCloud.tsx`

Expected: no matches.

Run: `git diff --check && git status --short`

Expected: no whitespace errors; only planned migration files and earlier migration work remain modified.
