{{> _header }}
  <div>
    {{#if google}}
      <p>你正在使用 Google 登录数据看板，因此没有单独的登录密码。请在登录页面点击“使用 Google 登录”。</p>
      <a href="{{siteUrl}}">进入数据看板</a>
    {{else}}
      {{#if nonGoogleSSO}}
        <p>你正在使用单点登录（SSO）进入数据看板，因此无法在此重置密码。请在登录页面点击“使用 SSO 登录”。如需修改密码，请联系系统管理员。</p>
        <a href="{{siteUrl}}">进入数据看板</a>
      {{else}}
        {{#if isActive}}
          <div style="text-align: center">
            <p>请点击下方按钮，重置你在 {{siteUrl}} 的数据看板账号密码。</p>
            <a style="display: inline-block; box-sizing: border-box; font-size: 18px; padding: 8px 22px; cursor: pointer; text-decoration: none; border-radius: 4px; background-color: #4990E2; border-color: #4990E2; color: #fff;" href="{{passwordResetUrl}}">重置密码</a>
            <p style="padding-top: 2em; font-size: small;">如果不是你本人发起的密码重置请求，请忽略此邮件。</p>
          </div>
        {{else}}
          <p>有人请求重置你的数据看板账号密码，但该账号已被停用。如需帮助，请联系系统管理员。</p>
          {{#if adminEmailSet}}
            <a href="mailto:{{adminEmail}}">联系系统管理员</a>
          {{/if}}
        {{/if}}
      {{/if}}
    {{/if}}
  </div>
{{> _footer }}
